import React from "react";
import styles from "../styles/Education.module.css";

export default function Home() {
  return (
    <section className={`mainRow ${styles.eduContainer}`}>
      <h2 className="sectionHeader">Education</h2>
    </section>
  );
}
