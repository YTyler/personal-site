import React from "react";
import styles from "../styles/Experience.module.css";

export default function Home() {
  return (
    <section className={`mainRow ${styles.expContainer}`}>
      <h2 className="sectionHeader">Experience</h2>
    </section>
  );
}
